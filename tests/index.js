"use strict";
exports.__esModule = true;
var el = {
    el: document.getElementById('test'),
    src: 'https://example.com'
};
var options = {};
var fn = {
    addData: function (el, options) {
    }
};
fn.addData(el, options);
